using SistemPlus.Code.DAL;
using SistemPlus.Code.DTO;
using MySql.Data.MySqlClient;
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace SistemPlus.Code.BLL.Fornecedor
{
    class PesquisaFornecedorBLL
    {
        /*
        conexaoBD bd = new conexaoBD();

        public DataTable ExibirDadosFornecedor()
        {
            CadastroFornecedorDTO dto = new CadastroFornecedorDTO();

            try
            {
                string comando = "SELECT * FROM fornecedor WHERE nome LIKE '" + dto.Nome + "':";

                bd.ExecutarComandoSQL(comando);

                //da.Fill(dt);

                //return dt;
            }
            catch (Exception ex)
            {
                throw ex;
            }
        }
        */
    }
}
